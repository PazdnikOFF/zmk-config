config SWEEP_DONGLE_UI
    bool "Экран донгла Sweep и каналы данных от мака"
    depends on ZMK_DISPLAY
    default y if SHIELD_SWEEP_DONGLE
    help
      Свой status screen вместо встроенного плюс приём метрик мака.
      Требует ZMK_DISPLAY_STATUS_SCREEN_CUSTOM.

config SWEEP_SPLIT_READVERTISE
    bool "Повторять рекламу половинки, пока нет связи с донглом"
    depends on ZMK_SPLIT_BLE && !ZMK_SPLIT_ROLE_CENTRAL
    default y
    help
      Лечит «после перезагрузки донгла половинка не цепляется сама, помогает
      только её ресет». ZMK перезапускает рекламу ровно один раз, а у
      половинки с бондом реклама направленная, и Zephyr отказывает ей с
      -EINVAL, пока жив объект старого соединения с донглом
      (le_adv_start_add_conn -> bt_conn_exists_le). Жив он, пока поток
      нотификаций ZMK держит на него ссылку в ожидании буфера — то есть ровно
      тогда, когда линк перед разрывом стоял. Системная очередь, в которой
      ZMK запускает рекламу, приоритетнее этого потока, так что отказ
      детерминирован, а повтора в ZMK нет (нет его и в main).

      Здесь, пока связи нет, раз в пару секунд запускается та же
      low-duty-направленная реклама, в которую ZMK переходит сам. Если она
      уже идёт, Zephyr отвечает -EALREADY и ничего не происходит.

config SWEEP_HALF_DEBUG_STATS
    bool "Логировать на половинке состояние отправки нажатий"
    depends on ZMK_SPLIT_BLE && !ZMK_SPLIT_ROLE_CENTRAL
    select HWINFO
    default n
    help
      Отвечает на «после перезагрузки донгла линк к половинке есть, а
      нажатий нет». Раз в пару секунд пишет, подписан ли донгл на нажатия
      (bt_gatt_is_subscribed), занята ли работа отправки ZMK и насколько
      заполнена очередь снимков. Подписки нет — не подписался донгл; подписка
      есть, а работа отправки висит — застряла сама половинка.

      Плюс причина последней перезагрузки, как у донгла. Нужен лог по USB,
      то есть снипет zmk-usb-logging.

if SWEEP_DONGLE_UI

config SWEEP_DONGLE_HOST_LINK_CDC
    bool "Приём метрик мака через USB CDC-ACM"
    depends on USB_CDC_ACM
    default y
    help
      Работает, пока донгл воткнут кабелем. Нужен узел host_cdc в оверлее.

config SWEEP_DONGLE_HOST_LINK_GATT
    bool "Приём метрик мака через свою GATT-характеристику"
    depends on ZMK_BLE
    default y
    help
      Работает и на батарее: сервис висит на том же BLE-соединении, которым
      донгл отдаёт HID хосту. Запись требует шифрования, то есть сопряжения.

config SWEEP_DONGLE_IDLE_SLOW_SPLIT
    bool "Растягивать интервал сплит-линков на простое"
    depends on ZMK_SPLIT_ROLE_CENTRAL
    default n
    help
      ВЫКЛЮЧЕНО ПОСЛЕ НЕУДАЧИ. Замысел был верный и экономия крупная:
      центральный обязан дежурить на каждом событии соединения, поэтому
      растянутый на простое интервал сокращает работу радио примерно в 66 раз.

      На практике донгл уходил на секундный интервал и обратно не возвращался:
      после пяти минут простоя ввод оставался вязким, пока не переподключишь.
      Тормозило даже при подключении по USB, то есть проблема именно на
      участке половинка-донгл, а не на линке к хосту.

      Причина скорее всего в том, что bt_conn_le_param_update при возврате к
      быстрым параметрам не отрабатывает, а повтора в коде нет — одна неудача
      оставляет связь медленной навсегда. Чтобы вернуть, нужно: повторять
      попытку до успеха, проверять фактический интервал через bt_conn_get_info
      и ускоряться не только по событию активности, но и по первому событию
      позиции.

config SWEEP_DONGLE_IDLE_SPLIT_INT
    int "Интервал сплит-линков на простое, единицы по 1.25 мс"
    depends on SWEEP_DONGLE_IDLE_SLOW_SPLIT
    default 800
    range 6 3200
    help
      800 единиц это ровно секунда. Центральный обязан дежурить на каждом
      событии соединения, поэтому растянутый интервал — самая крупная
      экономия из доступных. Цена: первое нажатие после простоя доедет с
      задержкой до одного интервала, дальше скорость обычная.

config SWEEP_DONGLE_IDLE_SPLIT_TIMEOUT
    int "Таймаут соединения на простое, единицы по 10 мс"
    depends on SWEEP_DONGLE_IDLE_SLOW_SPLIT
    default 600
    range 10 3200
    help
      Должен превышать удвоенный интервал простоя, иначе связь развалится.
      Проверяется BUILD_ASSERT в power.c.

config SWEEP_DONGLE_HOST_LINK_STARTUP_DELAY_MS
    int "Задержка перед включением приёма из CDC, мс"
    depends on SWEEP_DONGLE_HOST_LINK_CDC
    default 500
    help
      Устройство CDC-ACM появляется только после usb_enable(), который ZMK
      делает в SYS_INIT на уровне APPLICATION. Поток приёма стартует раньше,
      поэтому ждёт.

config SWEEP_DONGLE_DEBUG_LATENCY
    bool "Логировать время прихода нажатий с половинок"
    default n
    help
      Помечает момент прихода каждого события позиции. Ровные промежутки при
      вязкой печати означают, что тормозит участок донгл-хост, рваные —
      участок половинка-донгл.

config SWEEP_DONGLE_DEBUG_RESET_REASON
    bool "Логировать причину последней перезагрузки"
    select HWINFO
    default n
    help
      Отвечает на вопрос «почему донгл при подключении кабеля оказывается в
      бутлоадере». Бутлоадер уходит в UF2 по двойному ресету, то есть за этим
      всегда стоят две перезагрузки подряд, и важно понять, чьи они.

      Пустая причина означает, что питание пропадало физически — это просадка
      рейла при втыкании, а не ошибка прошивки. SOFTWARE, LOCKUP или WATCHDOG
      означают падение самой прошивки, и лог падения будет рядом. PIN — это
      кнопка, в том числе штатный двойной ресет.

config SWEEP_DONGLE_DEBUG_LINKS
    bool "Логировать состояние радиолинков: RSSI, параметры, разрывы, пачки"
    depends on ZMK_SPLIT_BLE && ZMK_SPLIT_ROLE_CENTRAL
    imply BT_CTLR_CONN_RSSI
    default n
    help
      Отвечает на вопрос «почему одна половинка со временем вязнет». Раз в
      10 секунд пишет по каждому соединению сторону, интервал, latency,
      таймаут и RSSI; каждый разрыв сплит-линка — сразу, с причиной; и
      «пачки» — события одной половинки, пришедшие не дальше 1 мс друг от
      друга, чего человек напечатать не может, а застрявший линк выдаёт.

      Плохой RSSI у одной половинки означает радио; нормальный RSSI при
      растущих пачках с одной стороны — планирование линков в донгле.

config SWEEP_DONGLE_DEBUG_REFRESH
    bool "Логировать площадь каждого обновления панели"
    default n
    help
      Вешает monitor_cb на драйвер дисплея и пишет в лог, сколько пикселей
      ушло в панель за цикл. Отличает «LVGL отдаёт слишком большую область» от
      «панель мигает целиком на своём частичном профиле».

endif
