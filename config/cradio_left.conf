# Донгл стал central, поэтому левая половина переводится в периферию.
# В самом шилде cradio_left стоит `default y`, а не жёсткое значение,
# так что переопределение здесь работает.
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
